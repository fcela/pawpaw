"""Version metadata.

__version__ is the package semver (used by PyPI, pip, etc.).
PIPELINE_VERSION is the cache-busting fingerprint — bump when behavior
changes in a way that should invalidate compiled caches.
"""
__version__ = "0.2.0"
PIPELINE_VERSION = "0.2.0"
